const theaters = require('./theaters/theaters')
const movies = require('./movies/movies')
const shows = require('./shows/shows')
const users = require('./users/users')
const bookTickets = require('./bookTickets/bookTickets')

module.exports = {
    theaters,
    movies,
    shows,
    users,
    bookTickets
}